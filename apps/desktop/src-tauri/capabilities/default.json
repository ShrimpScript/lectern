{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core APIs and the in-app updater for the main window.",
  "windows": ["main"],
  "permissions": ["core:default", "updater:default"]
}
