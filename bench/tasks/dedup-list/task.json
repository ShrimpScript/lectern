{
  "id": "dedup-list",
  "category": "feature",
  "prompt": "Create dedup.py with a function dedup(items) that removes duplicate elements while preserving first-seen order.",
  "grade": "python3 -c \"import dedup; assert dedup.dedup([1,2,1,3,2,4])==[1,2,3,4] and dedup.dedup([])==[] and dedup.dedup(['a','a','b'])==['a','b']\""
}
